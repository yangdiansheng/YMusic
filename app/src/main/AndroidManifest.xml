<manifest package="com.yangdiansheng.music"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:name=".MApplication"
        android:theme="@style/AppTheme">
        <activity android:name=".view.home.HomeActivity"
                  android:screenOrientation="portrait"
                  android:configChanges="keyboardHidden|screenSize|screenLayout|keyboard">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />

            </intent-filter>
        </activity>
        <activity android:name=".test.TestOneActivity"
            android:screenOrientation="portrait"
            android:configChanges="keyboardHidden|screenSize|screenLayout|keyboard"/>
        <activity android:name=".test.TestTwoActivity"
            android:screenOrientation="portrait"
            android:configChanges="keyboardHidden|screenSize|screenLayout|keyboard"/>
        <activity
            android:name=".view.login.LoginActivity"
            android:screenOrientation="portrait" />

    </application>
</manifest>
